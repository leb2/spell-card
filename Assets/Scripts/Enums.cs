using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum CardType { ELEMENT, SHAPE, MODIFIER, SPELL }
public enum ElementType { FIRE, ICE, ROT };
public enum ShapeType { LINE, CONE, CIRCLE, PROJECTILE };